<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="kr.co.hkcloud.palette3.core.security.authentication.dao.TalkAuthenticationMapper">
	<!-- 
	메뉴 권한 체크 ,custco_id 사용하지 않음
	 -->
	<select id="selectChkAuthMenu" parameterType="kr.co.hkcloud.palette3.core.security.authentication.domain.TalkAuthenticationVO" resultType="int">
	SELECT COUNT(1) AS hasCnt
	  FROM PLT_USER A
	     , PLT_USER_AUTH B
	     , PLT_AUTH C
	     , PLT_AUTH_PRG D
	 WHERE A.USER_ID       = B.USER_ID
	   AND B.ATRT_GROUP_ID = C.ATRT_GROUP_ID
	   AND C.ATRT_GROUP_ID = D.ATRT_GROUP_ID
	   AND A.USER_ID       = #{userId}
	   AND D.MENU_ID       = #{menuId}
	</select>
</mapper>