spring.config.activate:
  on-profile: "local-admin"
# ----------------------------------------
# amin Properties
# ----------------------------------------
spring:  
  #멀티테넌시(multitenancy) 처리 : flyway 설정
  flyway:
    enabled: false
    baseline-on-migrate: true
    baseline-version: 1
    locations: classpath:/db/migration
